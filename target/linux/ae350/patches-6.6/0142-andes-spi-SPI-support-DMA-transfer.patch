From 0ad626959fc794d8547d9b39d84aac0d9fd4879e Mon Sep 17 00:00:00 2001
From: locus84 <locus84@andestech.com>
Date: Wed, 6 Nov 2024 14:46:42 +0800
Subject: [PATCH 142/168] andes: spi: SPI support DMA transfer

Signed-off-by: locus84 <locus84@andestech.com>
---
 .../andes/a25_c1_d_dsp_noncoherent_ae350.dts  |   2 +
 .../boot/dts/andes/a25mp_c4_d_dsp_ae350.dts   |   2 +
 .../a27l2_c1_d_dsp_noncoherent_ae350.dts      |   2 +
 .../andes/a45_c1_d_dsp_noncoherent_ae350.dts  |   2 +
 .../boot/dts/andes/a45mp_c4_d_dsp_ae350.dts   |   2 +
 .../andes/ax25_c1_d_dsp_noncoherent_ae350.dts |   2 +
 .../dts/andes/ax25mp_amp_c2_64_d_ae350.dts    |   2 +
 .../boot/dts/andes/ax25mp_c4_d_dsp_ae350.dts  |   2 +
 .../ax27l2_c1_d_dsp_noncoherent_ae350.dts     |   2 +
 .../andes/ax45_c1_d_dsp_noncoherent_ae350.dts |   2 +
 .../boot/dts/andes/ax45mp_c4_d_dsp_ae350.dts  |   2 +
 arch/riscv/boot/dts/andes/ax45mp_c8_ae350.dts |   2 +
 .../boot/dts/andes/ax45mpv_c1_d_hvm_ae350.dts |   3 +
 .../dts/andes/ax45mpv_c2_d_dsp_hvm_ae350.dts  |   3 +
 .../boot/dts/andes/ax45mpv_c4_d_dsp_ae350.dts |   2 +
 .../boot/dts/andes/ax65mp_c2_d_ae350.dts      |   2 +
 arch/riscv/configs/andes-support.config       |   1 +
 drivers/spi/Kconfig                           |   8 +
 drivers/spi/Makefile                          |   1 +
 drivers/spi/spi-atcspi200-dma.c               | 187 ++++++++++++++++++
 drivers/spi/spi-atcspi200.c                   |  46 ++++-
 include/linux/soc/andes/spi-atcspi200.h       |  62 ++++--
 22 files changed, 314 insertions(+), 25 deletions(-)
 create mode 100644 drivers/spi/spi-atcspi200-dma.c

diff --git a/arch/riscv/boot/dts/andes/a25_c1_d_dsp_noncoherent_ae350.dts b/arch/riscv/boot/dts/andes/a25_c1_d_dsp_noncoherent_ae350.dts
index 13b950b73efe..9bc207d78b35 100644
--- a/arch/riscv/boot/dts/andes/a25_c1_d_dsp_noncoherent_ae350.dts
+++ b/arch/riscv/boot/dts/andes/a25_c1_d_dsp_noncoherent_ae350.dts
@@ -250,6 +250,8 @@ spi@f0b00000 {
 			#size-cells = <0x00>;
 			num-cs = <0x01>;
 			clocks = <0x03>;
+			dmas = <0x03 0>, <0x03 1>;
+			dma-names = "spi_tx", "spi_rx";
 
 			flash@0 {
 				compatible = "jedec,spi-nor";
diff --git a/arch/riscv/boot/dts/andes/a25mp_c4_d_dsp_ae350.dts b/arch/riscv/boot/dts/andes/a25mp_c4_d_dsp_ae350.dts
index 581421999384..a038e9f6de23 100644
--- a/arch/riscv/boot/dts/andes/a25mp_c4_d_dsp_ae350.dts
+++ b/arch/riscv/boot/dts/andes/a25mp_c4_d_dsp_ae350.dts
@@ -316,6 +316,8 @@ spi@f0b00000 {
 			#size-cells = <0x00>;
 			num-cs = <0x01>;
 			clocks = <0x08>;
+			dmas = <0x07 0>, <0x07 1>;
+			dma-names = "spi_tx", "spi_rx";
 
 			flash@0 {
 				compatible = "jedec,spi-nor";
diff --git a/arch/riscv/boot/dts/andes/a27l2_c1_d_dsp_noncoherent_ae350.dts b/arch/riscv/boot/dts/andes/a27l2_c1_d_dsp_noncoherent_ae350.dts
index 28358c794b32..3b585a0b5bad 100644
--- a/arch/riscv/boot/dts/andes/a27l2_c1_d_dsp_noncoherent_ae350.dts
+++ b/arch/riscv/boot/dts/andes/a27l2_c1_d_dsp_noncoherent_ae350.dts
@@ -262,6 +262,8 @@ spi@f0b00000 {
 			#size-cells = <0x00>;
 			num-cs = <0x01>;
 			clocks = <0x04>;
+			dmas = <0x05 0>, <0x05 1>;
+			dma-names = "spi_tx", "spi_rx";
 
 			flash@0 {
 				compatible = "jedec,spi-nor";
diff --git a/arch/riscv/boot/dts/andes/a45_c1_d_dsp_noncoherent_ae350.dts b/arch/riscv/boot/dts/andes/a45_c1_d_dsp_noncoherent_ae350.dts
index 4f33176fc996..9c79edd049e6 100644
--- a/arch/riscv/boot/dts/andes/a45_c1_d_dsp_noncoherent_ae350.dts
+++ b/arch/riscv/boot/dts/andes/a45_c1_d_dsp_noncoherent_ae350.dts
@@ -252,6 +252,8 @@ spi@f0b00000 {
 			#size-cells = <0x00>;
 			num-cs = <0x01>;
 			clocks = <0x04>;
+			dmas = <0x03 0>, <0x03 1>;
+			dma-names = "spi_tx", "spi_rx";
 
 			flash@0 {
 				compatible = "jedec,spi-nor";
diff --git a/arch/riscv/boot/dts/andes/a45mp_c4_d_dsp_ae350.dts b/arch/riscv/boot/dts/andes/a45mp_c4_d_dsp_ae350.dts
index 4ed243b8d2ca..9adc79cbf293 100644
--- a/arch/riscv/boot/dts/andes/a45mp_c4_d_dsp_ae350.dts
+++ b/arch/riscv/boot/dts/andes/a45mp_c4_d_dsp_ae350.dts
@@ -315,6 +315,8 @@ spi@f0b00000 {
 			#size-cells = <0x00>;
 			num-cs = <0x01>;
 			clocks = <0x08>;
+			dmas = <0x07 0>, <0x07 1>;
+			dma-names = "spi_tx", "spi_rx";
 
 			flash@0 {
 				compatible = "jedec,spi-nor";
diff --git a/arch/riscv/boot/dts/andes/ax25_c1_d_dsp_noncoherent_ae350.dts b/arch/riscv/boot/dts/andes/ax25_c1_d_dsp_noncoherent_ae350.dts
index ec533e6c6cb7..6f9914046296 100644
--- a/arch/riscv/boot/dts/andes/ax25_c1_d_dsp_noncoherent_ae350.dts
+++ b/arch/riscv/boot/dts/andes/ax25_c1_d_dsp_noncoherent_ae350.dts
@@ -250,6 +250,8 @@ spi@f0b00000 {
 			#size-cells = <0x00>;
 			num-cs = <0x01>;
 			clocks = <0x03>;
+			dmas = <0x03 0>, <0x03 1>;
+			dma-names = "spi_tx", "spi_rx";
 
 			flash@0 {
 				compatible = "jedec,spi-nor";
diff --git a/arch/riscv/boot/dts/andes/ax25mp_amp_c2_64_d_ae350.dts b/arch/riscv/boot/dts/andes/ax25mp_amp_c2_64_d_ae350.dts
index 454538277224..107a3f426126 100644
--- a/arch/riscv/boot/dts/andes/ax25mp_amp_c2_64_d_ae350.dts
+++ b/arch/riscv/boot/dts/andes/ax25mp_amp_c2_64_d_ae350.dts
@@ -299,6 +299,8 @@ spi@f0b00000 {
 		clocks = <0x5>;
 		interrupts = <0x4 0x4>;
 		interrupt-parent = <0x4>;
+		dmas = <0x07 0>, <0x07 1>;
+		dma-names = "spi_tx", "spi_rx";
 
 		flash@0 {
 			compatible = "mx25u1635e", "jedec,spi-nor";
diff --git a/arch/riscv/boot/dts/andes/ax25mp_c4_d_dsp_ae350.dts b/arch/riscv/boot/dts/andes/ax25mp_c4_d_dsp_ae350.dts
index a3c1b3b82cc2..f4cabc251060 100644
--- a/arch/riscv/boot/dts/andes/ax25mp_c4_d_dsp_ae350.dts
+++ b/arch/riscv/boot/dts/andes/ax25mp_c4_d_dsp_ae350.dts
@@ -316,6 +316,8 @@ spi@f0b00000 {
 			#size-cells = <0x00>;
 			num-cs = <0x01>;
 			clocks = <0x08>;
+			dmas = <0x07 0>, <0x07 1>;
+			dma-names = "spi_tx", "spi_rx";
 
 			flash@0 {
 				compatible = "jedec,spi-nor";
diff --git a/arch/riscv/boot/dts/andes/ax27l2_c1_d_dsp_noncoherent_ae350.dts b/arch/riscv/boot/dts/andes/ax27l2_c1_d_dsp_noncoherent_ae350.dts
index 74b3694ab498..1e463f8bec31 100644
--- a/arch/riscv/boot/dts/andes/ax27l2_c1_d_dsp_noncoherent_ae350.dts
+++ b/arch/riscv/boot/dts/andes/ax27l2_c1_d_dsp_noncoherent_ae350.dts
@@ -262,6 +262,8 @@ spi@f0b00000 {
 			#size-cells = <0x00>;
 			num-cs = <0x01>;
 			clocks = <0x04>;
+			dmas = <0x05 0>, <0x05 1>;
+			dma-names = "spi_tx", "spi_rx";
 
 			flash@0 {
 				compatible = "jedec,spi-nor";
diff --git a/arch/riscv/boot/dts/andes/ax45_c1_d_dsp_noncoherent_ae350.dts b/arch/riscv/boot/dts/andes/ax45_c1_d_dsp_noncoherent_ae350.dts
index e783d5099352..e1f11d9f0ee0 100644
--- a/arch/riscv/boot/dts/andes/ax45_c1_d_dsp_noncoherent_ae350.dts
+++ b/arch/riscv/boot/dts/andes/ax45_c1_d_dsp_noncoherent_ae350.dts
@@ -252,6 +252,8 @@ spi@f0b00000 {
 			#size-cells = <0x00>;
 			num-cs = <0x01>;
 			clocks = <0x04>;
+			dmas = <0x03 0>, <0x03 1>;
+			dma-names = "spi_tx", "spi_rx";
 
 			flash@0 {
 				compatible = "jedec,spi-nor";
diff --git a/arch/riscv/boot/dts/andes/ax45mp_c4_d_dsp_ae350.dts b/arch/riscv/boot/dts/andes/ax45mp_c4_d_dsp_ae350.dts
index c3c307cd66ac..078d680645aa 100644
--- a/arch/riscv/boot/dts/andes/ax45mp_c4_d_dsp_ae350.dts
+++ b/arch/riscv/boot/dts/andes/ax45mp_c4_d_dsp_ae350.dts
@@ -315,6 +315,8 @@ spi@f0b00000 {
 			#size-cells = <0x00>;
 			num-cs = <0x01>;
 			clocks = <0x08>;
+			dmas = <0x07 0>, <0x07 1>;
+			dma-names = "spi_tx", "spi_rx";
 
 			flash@0 {
 				compatible = "jedec,spi-nor";
diff --git a/arch/riscv/boot/dts/andes/ax45mp_c8_ae350.dts b/arch/riscv/boot/dts/andes/ax45mp_c8_ae350.dts
index c915a2d415db..48b538bbbbaa 100644
--- a/arch/riscv/boot/dts/andes/ax45mp_c8_ae350.dts
+++ b/arch/riscv/boot/dts/andes/ax45mp_c8_ae350.dts
@@ -419,6 +419,8 @@ spi@f0b00000 {
 			#size-cells = <0x00>;
 			num-cs = <0x01>;
 			clocks = <0x0c>;
+			dmas = <0x0b 0>, <0x0b 1>;
+			dma-names = "spi_tx", "spi_rx";
 
 			flash@0 {
 				compatible = "jedec,spi-nor";
diff --git a/arch/riscv/boot/dts/andes/ax45mpv_c1_d_hvm_ae350.dts b/arch/riscv/boot/dts/andes/ax45mpv_c1_d_hvm_ae350.dts
index 48d7222ee2b9..1c124143c942 100644
--- a/arch/riscv/boot/dts/andes/ax45mpv_c1_d_hvm_ae350.dts
+++ b/arch/riscv/boot/dts/andes/ax45mpv_c1_d_hvm_ae350.dts
@@ -199,6 +199,9 @@ spi: spi@f0b00000 {
 			#size-cells = <0>;
 			num-cs = <1>;
 			clocks = <&spiclk>;
+			dmas = <&dma0 0>, <&dma0 1>;
+			dma-names = "spi_tx", "spi_rx";
+
 			flash@0 {
 				compatible = "jedec,spi-nor";
 				reg = <0x00000000>;
diff --git a/arch/riscv/boot/dts/andes/ax45mpv_c2_d_dsp_hvm_ae350.dts b/arch/riscv/boot/dts/andes/ax45mpv_c2_d_dsp_hvm_ae350.dts
index 703e0a445eae..1cb75f9c76ad 100644
--- a/arch/riscv/boot/dts/andes/ax45mpv_c2_d_dsp_hvm_ae350.dts
+++ b/arch/riscv/boot/dts/andes/ax45mpv_c2_d_dsp_hvm_ae350.dts
@@ -225,6 +225,9 @@ spi: spi@f0b00000 {
 			#size-cells = <0>;
 			num-cs = <1>;
 			clocks = <&spiclk>;
+			dmas = <&dma0 0>, <&dma0 1>;
+			dma-names = "spi_tx", "spi_rx";
+
 			flash@0 {
 				compatible = "jedec,spi-nor";
 				reg = <0x00000000>;
diff --git a/arch/riscv/boot/dts/andes/ax45mpv_c4_d_dsp_ae350.dts b/arch/riscv/boot/dts/andes/ax45mpv_c4_d_dsp_ae350.dts
index 50234d33656e..0d7942a6733a 100644
--- a/arch/riscv/boot/dts/andes/ax45mpv_c4_d_dsp_ae350.dts
+++ b/arch/riscv/boot/dts/andes/ax45mpv_c4_d_dsp_ae350.dts
@@ -315,6 +315,8 @@ spi@f0b00000 {
 			#size-cells = <0x00>;
 			num-cs = <0x01>;
 			clocks = <0x08>;
+			dmas = <0x07 0>, <0x07 1>;
+			dma-names = "spi_tx", "spi_rx";
 
 			flash@0 {
 				compatible = "jedec,spi-nor";
diff --git a/arch/riscv/boot/dts/andes/ax65mp_c2_d_ae350.dts b/arch/riscv/boot/dts/andes/ax65mp_c2_d_ae350.dts
index 68d913dde85d..51422d1e59f4 100644
--- a/arch/riscv/boot/dts/andes/ax65mp_c2_d_ae350.dts
+++ b/arch/riscv/boot/dts/andes/ax65mp_c2_d_ae350.dts
@@ -258,6 +258,8 @@ spi@f0b00000 {
 			#size-cells = <0x00>;
 			num-cs = <0x01>;
 			clocks = <0x06>;
+			dmas = <0x05 0>, <0x05 1>;
+			dma-names = "spi_tx", "spi_rx";
 
 			flash@0 {
 				compatible = "jedec,spi-nor";
diff --git a/arch/riscv/configs/andes-support.config b/arch/riscv/configs/andes-support.config
index 721cdf6ecb35..04f32feda377 100644
--- a/arch/riscv/configs/andes-support.config
+++ b/arch/riscv/configs/andes-support.config
@@ -10,6 +10,7 @@ CONFIG_SND_FTSSP010=y
 CONFIG_SND_FTSSP010_AC97=y
 CONFIG_SPI=y
 CONFIG_SPI_ATCSPI200=y
+CONFIG_SPI_ATCSPI200_DMA=y
 CONFIG_MTD=y
 CONFIG_MTD_SPI_NOR=y
 
diff --git a/drivers/spi/Kconfig b/drivers/spi/Kconfig
index f4339b692cda..e9f5fcfad58e 100644
--- a/drivers/spi/Kconfig
+++ b/drivers/spi/Kconfig
@@ -1181,6 +1181,14 @@ config SPI_ATCSPI200
         depends on HAS_IOMEM
         help
           This exposes the SPI controller IP from Andes.
+
+config SPI_ATCSPI200_DMA
+	tristate "Enable DMA mode"
+	depends on SPI_ATCSPI200
+	help
+	  This option enables Direct Memory Access (DMA) mode
+	  for the ATCSPI200 driver, allowing more efficient data transfers
+	  by shifting the workload from the CPU to the DMA controller.
 #
 # Add new SPI master controllers in alphabetical order above this line
 #
diff --git a/drivers/spi/Makefile b/drivers/spi/Makefile
index e00cf64e23a5..6b6e631eca51 100644
--- a/drivers/spi/Makefile
+++ b/drivers/spi/Makefile
@@ -25,6 +25,7 @@ obj-$(CONFIG_SPI_ATMEL)			+= spi-atmel.o
 obj-$(CONFIG_SPI_ATMEL_QUADSPI)		+= atmel-quadspi.o
 obj-$(CONFIG_SPI_AT91_USART)		+= spi-at91-usart.o
 obj-$(CONFIG_SPI_ATCSPI200)		+= spi-atcspi200.o
+obj-$(CONFIG_SPI_ATCSPI200_DMA)		+= spi-atcspi200-dma.o
 obj-$(CONFIG_SPI_ATH79)			+= spi-ath79.o
 obj-$(CONFIG_SPI_AU1550)		+= spi-au1550.o
 obj-$(CONFIG_SPI_AXI_SPI_ENGINE)	+= spi-axi-spi-engine.o
diff --git a/drivers/spi/spi-atcspi200-dma.c b/drivers/spi/spi-atcspi200-dma.c
new file mode 100644
index 000000000000..219b1d6be6cf
--- /dev/null
+++ b/drivers/spi/spi-atcspi200-dma.c
@@ -0,0 +1,187 @@
+// SPDX-License-Identifier: GPL-2.0-only
+/*
+ * Copyright (C) 2024 Andes Technology Corporation.
+ */
+
+#include <linux/dmaengine.h>
+#include <linux/spi/spi-mem.h>
+#include <linux/soc/andes/spi-atcspi200.h>
+#define DMA_DEV_TIMEOUT_MS	1000
+
+int atcspi200_spi_dma_init(struct device *dev, struct atcspi200_spi *spi)
+{
+	int ret;
+	struct dma_slave_caps tx, rx;
+
+	if (IS_ENABLED(CONFIG_ATCSPI200_DMA_RX)) {
+		spi->rxchan = dma_request_chan(dev, "spi_rx");
+		if (IS_ERR(spi->rxchan)) {
+			ret = PTR_ERR(spi->rxchan);
+			spi->rxchan = NULL;
+			return ret;
+		}
+		spi->controller->dma_rx = spi->rxchan;
+		ret = dma_get_slave_caps(spi->rxchan, &rx);
+		if (ret)
+			return ret;
+	}
+	if (IS_ENABLED(CONFIG_ATCSPI200_DMA_TX)) {
+		spi->txchan = dma_request_chan(dev, "spi_tx");
+		if (IS_ERR(spi->txchan)) {
+			ret = PTR_ERR(spi->txchan);
+			spi->txchan = NULL;
+			return ret;
+		}
+		spi->controller->dma_tx = spi->txchan;
+		ret = dma_get_slave_caps(spi->txchan, &tx);
+		if (ret)
+			return ret;
+	}
+
+	init_completion(&spi->dma_completion);
+
+	return 0;
+}
+
+void atcspi200_spi_dma_exit(struct atcspi200_spi *spi)
+{
+	if (spi->txchan) {
+		dmaengine_terminate_sync(spi->txchan);
+		dma_release_channel(spi->txchan);
+	}
+
+	if (spi->rxchan) {
+		dmaengine_terminate_sync(spi->rxchan);
+		dma_release_channel(spi->rxchan);
+	}
+}
+
+static inline int spi_dma_config_rx(struct atcspi200_spi *spi)
+{
+	struct dma_slave_config rxconf = { 0 };
+
+	rxconf.direction = DMA_DEV_TO_MEM;
+	rxconf.src_addr = spi->dma_addr;
+	rxconf.dst_maxburst = 2;
+	rxconf.src_maxburst = 2;
+#ifdef	CONFIG_SPI_ATCSPI200_DATA_MERGE
+	rxconf.src_addr_width = DMA_SLAVE_BUSWIDTH_4_BYTES;
+	rxconf.dst_addr_width = DMA_SLAVE_BUSWIDTH_4_BYTES;
+#else
+	rxconf.src_addr_width = DMA_SLAVE_BUSWIDTH_1_BYTE;
+	rxconf.dst_addr_width = DMA_SLAVE_BUSWIDTH_1_BYTE;
+#endif
+
+	return dmaengine_slave_config(spi->rxchan, &rxconf);
+}
+
+static inline int spi_dma_config_tx(struct atcspi200_spi *spi)
+{
+	struct dma_slave_config txconf = { 0 };
+
+	txconf.direction = DMA_MEM_TO_DEV;
+	txconf.dst_addr = spi->dma_addr;
+	txconf.dst_maxburst = 2;
+	txconf.src_maxburst = 2;
+#ifdef	CONFIG_SPI_ATCSPI200_DATA_MERGE
+	txconf.src_addr_width = DMA_SLAVE_BUSWIDTH_4_BYTES;
+	txconf.dst_addr_width = DMA_SLAVE_BUSWIDTH_4_BYTES;
+#else
+	txconf.src_addr_width = DMA_SLAVE_BUSWIDTH_1_BYTE;
+	txconf.dst_addr_width = DMA_SLAVE_BUSWIDTH_1_BYTE;
+#endif
+
+	return dmaengine_slave_config(spi->txchan, &txconf);
+}
+
+static void atcspi200_spi_dma_callback(void *arg)
+{
+	struct completion *dma_completion = arg;
+
+	complete(dma_completion);
+}
+
+int atcspi200_spi_dma_setup(struct atcspi200_spi *spi, struct spi_mem_op *op)
+{
+	int ret;
+	struct dma_async_tx_descriptor *desc;
+	struct dma_chan *dma_ch;
+	enum dma_transfer_direction dma_dir;
+	struct sg_table sgt;
+	dma_cookie_t cookie;
+
+	/* Setup DMA channels */
+	if (op->data.dir == SPI_MEM_DATA_IN) {
+		ret = spi_dma_config_rx(spi);
+		if (ret)
+			return ret;
+		dma_dir = DMA_DEV_TO_MEM;
+		dma_ch = spi->rxchan;
+	} else {
+		spi_dma_config_tx(spi);
+		if (ret)
+			return ret;
+		dma_dir = DMA_MEM_TO_DEV;
+		dma_ch = spi->txchan;
+	}
+
+	ret = spi_controller_dma_map_mem_op_data(spi->controller, op, &sgt);
+	if (ret)
+		return ret;
+
+	desc = dmaengine_prep_slave_sg(dma_ch, sgt.sgl, sgt.nents,
+				       dma_dir, DMA_PREP_INTERRUPT | DMA_CTRL_ACK);
+	if (!desc) {
+		ret = -ENOMEM;
+		goto exit_unmap;
+	}
+
+	reinit_completion(&spi->dma_completion);
+	desc->callback = atcspi200_spi_dma_callback;
+	desc->callback_param = &spi->dma_completion;
+
+	cookie = dmaengine_submit(desc);
+	ret = dma_submit_error(cookie);
+	if (ret)
+		goto exit_unmap;
+
+	return 0;
+
+exit_unmap:
+	spi_controller_dma_unmap_mem_op_data(spi->controller, op, &sgt);
+	return ret;
+}
+
+int atcspi200_spi_dma_transfer(struct atcspi200_spi *spi, struct spi_mem_op *op)
+{
+	struct dma_chan *dma_ch;
+	int ret, timeout = DMA_DEV_TIMEOUT_MS;
+
+	if (op->data.dir == SPI_MEM_DATA_IN)
+		dma_ch = spi->rxchan;
+	else
+		dma_ch = spi->txchan;
+
+	dma_async_issue_pending(dma_ch);
+	timeout = msecs_to_jiffies(op->data.nbytes * DMA_DEV_TIMEOUT_MS);
+	if (!wait_for_completion_timeout(&spi->dma_completion, timeout)) {
+		ret = -ETIMEDOUT;
+		return ret;
+	}
+
+	return 0;
+}
+
+static const struct atcspi200_dma_ops atcspi200_dma_ops = {
+	.dma_init = atcspi200_spi_dma_init,
+	.dma_exit = atcspi200_spi_dma_exit,
+	.dma_setup = atcspi200_spi_dma_setup,
+	.dma_transfer = atcspi200_spi_dma_transfer,
+};
+
+void atcspi200_spi_dma_ops_setup(struct atcspi200_spi *spi)
+{
+	if (IS_ENABLED(CONFIG_SPI_ATCSPI200_DMA) &&
+	    (IS_ENABLED(CONFIG_DMA_RX_OPTION) || IS_ENABLED(CONFIG_DMA_TX_OPTION)))
+		spi->dma_ops = &atcspi200_dma_ops;
+}
diff --git a/drivers/spi/spi-atcspi200.c b/drivers/spi/spi-atcspi200.c
index cbc4a77b29c2..a669b2b87491 100644
--- a/drivers/spi/spi-atcspi200.c
+++ b/drivers/spi/spi-atcspi200.c
@@ -346,6 +346,7 @@ static void atcspi200_set_transfer_ctl(struct atcspi200_spi *spi,
 				       const struct spi_mem_op *op)
 {
 	int tc = 0;
+
 	if (op->cmd.nbytes)
 		tc |= CMD_EN(op->cmd.nbytes);
 	if (op->addr.nbytes) {
@@ -396,9 +397,6 @@ static int atcspi200_spi_setting(struct atcspi200_spi *spi,
 	if (op->addr.nbytes)
 		atcspi200_spi_write(spi, SPI_ADDR, op->addr.val);
 
-	/* Write cmd to start SPI. */
-	atcspi200_spi_write(spi, SPI_CMD, op->cmd.opcode);
-
 	return 0;
 }
 
@@ -407,6 +405,12 @@ static int atcspi200_nor_adjust_op_size(struct spi_mem *mem,
 {
 	op->data.nbytes = min(op->data.nbytes, MAX_TRANSFER_LEN);
 
+	/*
+	 * DMA needs to be aligned to 4 byte
+	 */
+	if (op->data.nbytes < MAX_TRANSFER_LEN)
+		op->data.nbytes &= ~0x3;
+
 	return 0;
 }
 
@@ -421,8 +425,10 @@ static int atcspi200_exec_mem_op(struct spi_mem *mem, const struct spi_mem_op *o
 	struct spi_device *atcspi200_spi = mem->spi;
 	struct atcspi200_spi *spi = spi_controller_get_devdata(atcspi200_spi->controller);
 	unsigned int format_val;
+	int ctrl;
 
 	mutex_lock(&spi->mutex_lock);
+	ctrl = atcspi200_spi_read(spi, SPI_CTRL);
 	/* Check spi status. */
 	atcspi200_polling_spiactive(spi);
 	format_val = atcspi200_spi_read(spi, SPI_TRANSFMT);
@@ -447,10 +453,27 @@ static int atcspi200_exec_mem_op(struct spi_mem *mem, const struct spi_mem_op *o
 
 	/* set transfer length and data information. */
 	ret = atcspi200_spi_setting(spi, op);
+	if (!op->data.nbytes || op->data.nbytes < 0x100)
+		atcspi200_spi_write(spi, SPI_CMD, op->cmd.opcode);
 	/* Transfer data */
-	if (op->data.nbytes)
-		transfer_data(spi, spi->din, spi->dout, op->data.nbytes);
-	atcspi200_spi_stop(spi);
+	if (op->data.nbytes) {
+		if (spi->dma_ops->dma_transfer && op->data.nbytes >= 0x100) {
+			ret = spi->dma_ops->dma_setup(spi, op);
+			if (ret)
+				return ret;
+			ctrl = ctrl | ATCSPI200_CTRL_TXDMAEN | ATCSPI200_CTRL_RXDMAEN;
+			atcspi200_spi_write(spi, SPI_CTRL, ctrl);
+			atcspi200_spi_write(spi, SPI_CMD, op->cmd.opcode);
+			ret = spi->dma_ops->dma_transfer(spi, op);
+			if (ret)
+				printk("DMA transfer not finished\n");
+		} else {
+			transfer_data(spi, spi->din, spi->dout, op->data.nbytes);
+		}
+	}
+	ret = atcspi200_spi_stop(spi);
+	if (ret)
+		printk("timeout transfer not finished\n");
 	mutex_unlock(&spi->mutex_lock);
 
 	return ret;
@@ -480,11 +503,13 @@ static int atcspi200_spi_probe(struct platform_device *pdev)
 	}
 
 	spi = spi_controller_get_devdata(controller);
+	spi->controller = controller;
 	spin_lock_init(&spi->lock);
 	platform_set_drvdata(pdev, controller);
 
 	/* get base addr */
 	res = platform_get_resource(pdev, IORESOURCE_MEM, 0);
+	spi->dma_addr = (dma_addr_t)(res->start + SPI_DATA);
 	spi->regs = devm_ioremap_resource(&pdev->dev, res);
 
 	if (IS_ERR(spi->regs)) {
@@ -571,6 +596,13 @@ static int atcspi200_spi_probe(struct platform_device *pdev)
 	atcspi200_spi_setup(spi, subnode);
 	spi->mtiming = atcspi200_spi_read(spi, SPI_TIMING);
 
+	atcspi200_spi_dma_ops_setup(spi);
+	if (spi->dma_ops && spi->dma_ops->dma_init) {
+		ret = spi->dma_ops->dma_init(&pdev->dev, spi);
+		if (ret)
+			dev_warn(&pdev->dev, "DMA initialization failed, using PIO transfer\n");
+	}
+
 	ret = devm_request_irq(&pdev->dev, irq, andes_spi_irq, 0,
 			       "andes-spi", spi);
 
@@ -583,6 +615,8 @@ static int atcspi200_spi_probe(struct platform_device *pdev)
 	ret = devm_spi_register_controller(&pdev->dev, controller);
 	if (ret < 0) {
 		dev_err(&pdev->dev, "spi_register_controller failed\n");
+		if (spi->dma_ops && spi->dma_ops->dma_exit)
+			spi->dma_ops->dma_exit(spi);
 		goto put_controller;
 	}
 	dev_info(&pdev->dev, "Andes SPI atcspi200 driver.\n");
diff --git a/include/linux/soc/andes/spi-atcspi200.h b/include/linux/soc/andes/spi-atcspi200.h
index 831467c6ec40..c7d8b48ad615 100644
--- a/include/linux/soc/andes/spi-atcspi200.h
+++ b/include/linux/soc/andes/spi-atcspi200.h
@@ -6,6 +6,10 @@
 #ifndef __LINUX_SOC_ANDES_ATCSPI_H
 #define __LINUX_SOC_ANDES_ATCSPI_H
 
+#include <linux/spinlock.h>
+#include <linux/spi/spi-mem.h>
+#include <linux/types.h>
+
 #define SPI_XFER_BEGIN		(1 << 0)
 #define SPI_XFER_END		(1 << 1)
 #define SPI_XFER_DATA		(1 << 2)
@@ -47,6 +51,8 @@
 #define CMD_EN(x)				((x == 0) ? 0 : (1 << 30))
 
 /* SPI Control Register */
+#define ATCSPI200_CTRL_TXDMAEN			(1 << 4)
+#define ATCSPI200_CTRL_RXDMAEN			(1 << 3)
 #define ATCSPI200_CTRL_TXFIFORST_MASK		(1 << 2)
 #define ATCSPI200_CTRL_RXFIFORST_MASK		(1 << 1)
 #define ATCSPI200_CTRL_SPIRST_MASK		(1 << 0)
@@ -98,26 +104,44 @@ struct ts_buf {
 
 static bool ts_enable;
 
+struct atcspi200_spi;
+struct atcspi200_dma_ops {
+	int (*dma_init)(struct device *dev, struct atcspi200_spi *spi);
+	void (*dma_exit)(struct atcspi200_spi *spi);
+	int (*dma_setup)(struct atcspi200_spi *spi, struct spi_mem_op *op);
+	int (*dma_transfer)(struct atcspi200_spi *spi, struct spi_mem_op *op);
+	void (*dma_stop)(struct atcspi200_spi *spi);
+};
+
 struct atcspi200_spi {
-	void __iomem	*regs;
-	struct clk	*clk;
-	size_t		trans_len;
-	size_t		data_len;
-	size_t		cmd_len;
-	u32		clk_rate;
-	u8		cmd_buf[16];
-	u8		*din;
-	u8		*dout;
-	struct ts_buf	*tx_buf;
-	struct ts_buf	*rx_buf;
-	unsigned int	addr;
-	unsigned int	max_transfer_length;
-	unsigned int	freq;
-	unsigned int	mode;
-	unsigned int	mtiming;
-	int		timeout;
-	spinlock_t	lock;
-	struct mutex	mutex_lock;
+	struct spi_controller	*controller;
+	void __iomem		*regs;
+	struct clk		*clk;
+	size_t			trans_len;
+	size_t			data_len;
+	size_t			cmd_len;
+	u32			clk_rate;
+	u8			cmd_buf[16];
+	u32			*din;
+	u32			*dout;
+	struct ts_buf		*tx_buf;
+	struct ts_buf		*rx_buf;
+	unsigned int		addr;
+	unsigned int		max_transfer_length;
+	unsigned int		freq;
+	unsigned int		mode;
+	unsigned int		mtiming;
+	int			timeout;
+	spinlock_t		lock;
+	struct mutex		mutex_lock;
+
+	/* DMA info */
+	struct dma_chan		*txchan;
+	struct dma_chan		*rxchan;
+	dma_addr_t		dma_addr;
+	const struct atcspi200_dma_ops	*dma_ops;
+	struct completion	dma_completion;
 };
 
+void atcspi200_spi_dma_ops_setup(struct atcspi200_spi *spi);
 #endif /* !__LINUX_SOC_ANDES_ATCSPI_H */
-- 
2.34.1

